PORT=4000
JWT_SECRET=change-me-to-a-long-random-string-in-development
# Comma-separated allowed origins in production. In development, any
# http://localhost:<port> or http://127.0.0.1:<port> is also allowed.
CLIENT_ORIGIN=http://localhost:3000
